function findLeapYear (limit){
    var d = new date();
    var leapyears = [];
    for(var i= d.getfullyear.length;leapyears.length< limit; i++){
        if(i % 4===0 && i % 100 !== 0){
            
        }
        console.log(i);
    }
}