var names = ["Anna", "Bill", "Charlay", "Dexter", "Emily", "Frank", "George", "Hal", "Isaac", "Jill"];  

 var attemptCall = function(name, dontCall, call, sendText) {

     for (var i = 0; i < names.length; i++){ 
         if (names[i].length %2  ===0){ 
          console.log("odd");
         }
         else if (names[i].length %2  !=0){
              console.log("dont call"); 
         } 
             var str=names[i];
         else if(str.chartAt(name[i])==a){
             console.log("send message" + send()); 
         }
 }
     
   
     
 var callD = function(dontCall) {
   console.log(" Dont Call the name"+ name); 
 }
 var calls = function(call) {
   console.log("Call the name"+ name);

 }
  var send = function(sendText) {
   console.log("send message is"+ ...);
 
}

//var name = new attemptCall(calls());
attemptCall (name, dontCall,call,sendText)      
//name.callD(name.calls, name);