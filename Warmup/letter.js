
var char = "hello";
var  array = [];
var str ="";
for (var i = 0; i < array.length; i++){
    if (upCaps(array[i]==)){
        console.log(char);str = 
    // newstr = newstr + upCaps(array[i]
   }
  else if (upCaps(array[i]==)){
        console.log(charr);
    }
}
    function lowCaps(char) {
    return char = char.toUpperCase();
      
}
      function lowCaps(char) {
    return char = char.toLowerCase();
      }